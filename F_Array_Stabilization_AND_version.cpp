#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    long long tc=1;
    cin>>tc;
    while(tc--)
    {
        long long n;
        cin>>n;

        
    }
    return 0;
}